<!-- Topbar -->
<nav
  class="navbar navbar-expand navbar-light bg-white topbar mb-4 static-top shadow"
>
  <!-- Sidebar Toggle (Topbar) -->
  <button
    id="sidebarToggleTop"
    class="btn btn-link d-md-none rounded-circle mr-3"
    title="Toggle Sidebar"
  >
    <i class="fa fa-bars"></i>
  </button>

  <!-- Topbar Search -->
  <form
    class="d-none d-sm-inline-block form-inline mr-auto ml-md-3 my-2 my-md-0 mw-100 navbar-search"
  >
    <!-- <div class="input-group">
      <input
        type="text"
        class="form-control bg-light border-0 small"
        placeholder="Cari..."
        aria-label="Search"
        aria-describedby="basic-addon2"
      />
      <div class="input-group-append">
        <button class="btn btn-primary" type="button" title="Cari">
          <i class="fas fa-search fa-sm"></i>
        </button>
      </div>
    </div> -->
  </form>

  <!-- Topbar Navbar -->
  <ul class="navbar-nav ml-auto">
    <!-- Nav Item - Usaha Dropdown -->
    <li class="nav-item dropdown no-arrow mx-1">
      <a
        class="nav-link dropdown-toggle"
        href="#"
        id="usahaDropdown"
        role="button"
        data-toggle="dropdown"
        aria-haspopup="true"
        aria-expanded="false"
      >
        <i class="fas fa-store fa-fw"></i>
        <span class="ml-2 d-none d-lg-inline text-gray-600 small">Usaha</span>
      </a>
      <!-- Dropdown - Usaha -->
      <div
        class="dropdown-menu dropdown-menu-right shadow animated--grow-in"
        aria-labelledby="usahaDropdown"
      >
        <h6 class="dropdown-header">Filter Usaha</h6>
        <a class="dropdown-item" href="/menu">Tampilkan Semua</a>
        <% if (typeof usahaList !== 'undefined' && usahaList.length > 0) { %> <%
        usahaList.forEach(function(usaha) { %>
        <a class="dropdown-item" href="/menu?usaha_id=<%= usaha.id %>">
          <%= usaha.nama_usaha %>
        </a>
        <% }); %> <% } else { %>
        <a class="dropdown-item" href="#">Tidak ada usaha tersedia</a>
        <% } %>
      </div>
    </li>

    <!-- Nav Item - Pengeluaran Usaha Dropdown -->
    <li class="nav-item dropdown no-arrow mx-1">
      <a
        class="nav-link dropdown-toggle"
        href="#"
        id="pengeluaranUsahaDropdown"
        role="button"
        data-toggle="dropdown"
        aria-haspopup="true"
        aria-expanded="false"
      >
        <i class="fas fa-money-bill-wave fa-fw"></i>
        <span class="ml-2 d-none d-lg-inline text-gray-600 small"
          >Pengeluaran Usaha</span
        >
      </a>
      <!-- Dropdown - Pengeluaran Usaha -->
      <div
        class="dropdown-menu dropdown-menu-right shadow animated--grow-in"
        aria-labelledby="pengeluaranUsahaDropdown"
      >
        <h6 class="dropdown-header">Filter Pengeluaran Usaha</h6>
        <a class="dropdown-item" href="/pengeluaran">Tampilkan Semua</a>
        <% if (typeof usahaList !== 'undefined' && usahaList.length > 0) { %> <%
        usahaList.forEach(function(usaha) { %>
        <a class="dropdown-item" href="/pengeluaran?usaha_id=<%= usaha.id %>">
          <%= usaha.nama_usaha %>
        </a>
        <% }); %> <% } else { %>
        <a class="dropdown-item" href="#">Tidak ada usaha tersedia</a>
        <% } %>
      </div>
    </li>

    <!-- Nav Item - Barang Usaha Dropdown -->
    <li class="nav-item dropdown no-arrow mx-1">
      <a
        class="nav-link dropdown-toggle"
        href="#"
        id="barangUsahaDropdown"
        role="button"
        data-toggle="dropdown"
        aria-haspopup="true"
        aria-expanded="false"
      >
        <i class="fas fa-boxes fa-fw"></i>
        <span class="ml-2 d-none d-lg-inline text-gray-600 small"
          >Barang Usaha</span
        >
      </a>
      <!-- Dropdown - Barang Usaha -->
      <div
        class="dropdown-menu dropdown-menu-right shadow animated--grow-in"
        aria-labelledby="barangUsahaDropdown"
      >
        <h6 class="dropdown-header">Filter Barang Usaha</h6>
        <a class="dropdown-item" href="/barang">Tampilkan Semua</a>
        <% if (typeof usahaList !== 'undefined' && usahaList.length > 0) { %> <%
        usahaList.forEach(function(usaha) { %>
        <a class="dropdown-item" href="/barang?usaha_id=<%= usaha.id %>">
          <%= usaha.nama_usaha %>
        </a>
        <% }); %> <% } else { %>
        <a class="dropdown-item" href="#">Tidak ada usaha tersedia</a>
        <% } %>
      </div>
    </li>

    <!-- Nav Item - Pemasukan Usaha Dropdown -->
    <li class="nav-item dropdown no-arrow mx-1">
      <a
        class="nav-link dropdown-toggle"
        href="#"
        id="pemasukanUsahaDropdown"
        role="button"
        data-toggle="dropdown"
        aria-haspopup="true"
        aria-expanded="false"
      >
        <i class="fas fa-money-check-alt fa-fw"></i>
        <span class="ml-2 d-none d-lg-inline text-gray-600 small"
          >Pemasukan Usaha</span
        >
      </a>
      <!-- Dropdown - Pemasukan Usaha -->
      <div
        class="dropdown-menu dropdown-menu-right shadow animated--grow-in"
        aria-labelledby="pemasukanUsahaDropdown"
      >
        <h6 class="dropdown-header">Filter Pemasukan Usaha</h6>
        <a class="dropdown-item" href="/pemasukan">Tampilkan Semua</a>
        <% if (typeof usahaList !== 'undefined' && usahaList.length > 0) { %> <%
        usahaList.forEach(function(usaha) { %>
        <a class="dropdown-item" href="/pemasukan?usaha_id=<%= usaha.id %>">
          <%= usaha.nama_usaha %>
        </a>
        <% }); %> <% } else { %>
        <a class="dropdown-item" href="#">Tidak ada usaha tersedia</a>
        <% } %>
      </div>
    </li>

    <!-- Nav Item - Search Dropdown (Visible Only XS) -->
    <li class="nav-item dropdown no-arrow d-sm-none">
      <a
        class="nav-link dropdown-toggle"
        href="#"
        id="searchDropdown"
        role="button"
        data-toggle="dropdown"
        aria-haspopup="true"
        aria-expanded="false"
      >
        <i class="fas fa-search fa-fw"></i>
      </a>
      <!-- Dropdown - Messages -->
      <div
        class="dropdown-menu dropdown-menu-right p-3 shadow animated--grow-in"
        aria-labelledby="searchDropdown"
      >
        <form class="form-inline mr-auto w-100 navbar-search">
          <div class="input-group">
            <input
              type="text"
              class="form-control bg-light border-0 small"
              placeholder="Cari..."
              aria-label="Search"
              aria-describedby="basic-addon2"
            />
            <div class="input-group-append">
              <button class="btn btn-primary" type="button" title="Cari">
                <i class="fas fa-search fa-sm"></i>
              </button>
            </div>
          </div>
        </form>
      </div>
    </li>

    <!-- Nav Item - Alerts -->
    <li class="nav-item dropdown no-arrow mx-1">
      <a
        class="nav-link dropdown-toggle"
        href="#"
        id="alertsDropdown"
        role="button"
        data-toggle="dropdown"
        aria-haspopup="true"
        aria-expanded="false"
      >
        <i class="fas fa-bell fa-fw"></i>
        <!-- Counter - Alerts -->
        <span class="badge badge-danger badge-counter"></span>
      </a>
      <!-- Dropdown - Alerts -->
      <div
        class="dropdown-list dropdown-menu dropdown-menu-right shadow animated--grow-in"
        aria-labelledby="alertsDropdown"
        id="notificationDropdownContent"
      >
        <h6 class="dropdown-header">Notifikasi</h6>
        <!-- Notifikasi dinamis akan dimasukkan di sini oleh JavaScript -->
        <a class="dropdown-item text-center small text-gray-500" href="#"
          >Tampilkan Semua Notifikasi</a
        >
      </div>
    </li>

    <!-- Nav Item - User Information -->
    <li class="nav-item dropdown no-arrow">
      <a
        class="nav-link dropdown-toggle"
        href="#"
        id="userDropdown"
        role="button"
        data-toggle="dropdown"
        aria-haspopup="true"
        aria-expanded="false"
      >
        <span class="mr-2 d-none d-lg-inline text-gray-600 small">Admin</span>
        <img
          class="img-profile rounded-circle"
          src="/img/undraw_profile.svg"
          alt="Profile"
        />
      </a>
      <!-- Dropdown - User Information -->
      <div
        class="dropdown-menu dropdown-menu-right shadow animated--grow-in"
        aria-labelledby="userDropdown"
      >
        <a class="dropdown-item" href="#">
          <i class="fas fa-user fa-sm fa-fw mr-2 text-gray-400"></i>
          Profile
        </a>
        <a class="dropdown-item" href="#">
          <i class="fas fa-cogs fa-sm fa-fw mr-2 text-gray-400"></i>
          Settings
        </a>
        <div class="dropdown-divider"></div>
        <a
          class="dropdown-item"
          href="#"
          data-toggle="modal"
          data-target="#logoutModal"
        >
          <i class="fas fa-sign-out-alt fa-sm fa-fw mr-2 text-gray-400"></i>
          Logout
        </a>
      </div>
    </li>
  </ul>
</nav>
<!-- End of Topbar -->
